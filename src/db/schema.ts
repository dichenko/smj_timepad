import { pgTable, serial, varchar, text, integer, boolean, timestamp, jsonb, uniqueIndex, index, pgEnum } from "drizzle-orm/pg-core";
export const participantStatus = pgEnum("participant_status", ["ACTIVE", "DEACTIVATED"]);
export const registrationStatus = pgEnum("registration_status", ["ACTIVE", "CANCELLED"]);
export const createdBy = pgEnum("created_by", ["PARTICIPANT", "ADMIN"]);
const dates = { createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(), updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow().notNull() };
export const conferences = pgTable("conferences", { id: serial("id").primaryKey(), title: varchar("title", { length: 200 }).notNull(), slug: varchar("slug", { length: 100 }).notNull().unique(), description: text("description"), location: varchar("location", { length: 200 }), conferenceDate: timestamp("conference_date", { withTimezone: true }), registrationOpen: boolean("registration_open").default(true).notNull(), registrationOpensAt: timestamp("registration_opens_at", { withTimezone: true }), registrationClosesAt: timestamp("registration_closes_at", { withTimezone: true }), ...dates });
export const timeSlots = pgTable("time_slots", { id: serial("id").primaryKey(), conferenceId: integer("conference_id").notNull().references(() => conferences.id), title: varchar("title", { length: 200 }).notNull(), startsAt: timestamp("starts_at", { withTimezone: true }).notNull(), endsAt: timestamp("ends_at", { withTimezone: true }).notNull(), sortOrder: integer("sort_order").notNull(), ...dates });
export const events = pgTable("events", { id: serial("id").primaryKey(), conferenceId: integer("conference_id").notNull().references(() => conferences.id), timeSlotId: integer("time_slot_id").notNull().references(() => timeSlots.id), title: varchar("title", { length: 250 }).notNull(), description: text("description").notNull(), speakerName: varchar("speaker_name", { length: 150 }).notNull(), speakerPhotoPath: varchar("speaker_photo_path", { length: 500 }), room: varchar("room", { length: 100 }), capacity: integer("capacity").notNull(), bookedCount: integer("booked_count").default(0).notNull(), registrationOpen: boolean("registration_open").default(true).notNull(), sortOrder: integer("sort_order").notNull(), ...dates });
export const participants = pgTable("participants", { id: serial("id").primaryKey(), conferenceId: integer("conference_id").notNull().references(() => conferences.id), ticketNumber: varchar("ticket_number", { length: 50 }).notNull(), firstName: varchar("first_name", { length: 100 }).notNull(), lastName: varchar("last_name", { length: 100 }).notNull(), city: varchar("city", { length: 150 }).notNull(), email: varchar("email", { length: 254 }).notNull(), normalizedEmail: varchar("normalized_email", { length: 254 }).notNull(), ticketTokenHash: varchar("ticket_token_hash", { length: 64 }).notNull(), status: participantStatus("status").default("ACTIVE").notNull(), ...dates }, t => ({ emailUnique: uniqueIndex("participants_conference_email_unique").on(t.conferenceId, t.normalizedEmail), ticketUnique: uniqueIndex("participants_ticket_number_unique").on(t.ticketNumber), tokenUnique: uniqueIndex("participants_token_hash_unique").on(t.ticketTokenHash) }));
export const registrations = pgTable("registrations", { id: serial("id").primaryKey(), participantId: integer("participant_id").notNull().references(() => participants.id), eventId: integer("event_id").notNull().references(() => events.id), timeSlotId: integer("time_slot_id").notNull().references(() => timeSlots.id), status: registrationStatus("status").default("ACTIVE").notNull(), createdBy: createdBy("created_by").notNull(), cancelledAt: timestamp("cancelled_at", { withTimezone: true }), ...dates }, t => ({ event: index("registrations_event_idx").on(t.eventId) }));
export const admins = pgTable("admins", { id: serial("id").primaryKey(), login: varchar("login", { length: 100 }).notNull().unique(), passwordHash: text("password_hash").notNull(), active: boolean("active").default(true).notNull(), lastLoginAt: timestamp("last_login_at", { withTimezone: true }), ...dates });
export const adminSessions = pgTable("admin_sessions", { id: serial("id").primaryKey(), adminId: integer("admin_id").notNull().references(() => admins.id), sessionTokenHash: varchar("session_token_hash", { length: 64 }).notNull().unique(), expiresAt: timestamp("expires_at", { withTimezone: true }).notNull(), lastUsedAt: timestamp("last_used_at", { withTimezone: true }).defaultNow().notNull(), ipAddress: varchar("ip_address", { length: 64 }), userAgent: varchar("user_agent", { length: 500 }), createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull() });
export const idempotencyKeys = pgTable("idempotency_keys", { id: serial("id").primaryKey(), key: varchar("key", { length: 100 }).notNull(), operation: varchar("operation", { length: 50 }).notNull(), responseData: jsonb("response_data"), createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(), expiresAt: timestamp("expires_at", { withTimezone: true }).notNull() }, t => ({ keyUnique: uniqueIndex("idempotency_operation_key_unique").on(t.operation, t.key) }));
export const auditLogs = pgTable("audit_logs", { id: serial("id").primaryKey(), adminId: integer("admin_id").references(() => admins.id), action: varchar("action", { length: 100 }).notNull(), entityType: varchar("entity_type", { length: 50 }).notNull(), entityId: varchar("entity_id", { length: 100 }).notNull(), oldData: jsonb("old_data"), newData: jsonb("new_data"), ipAddress: varchar("ip_address", { length: 64 }), userAgent: varchar("user_agent", { length: 500 }), createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull() });
