import { describe,it,expect } from "vitest"; import { clean,places,registrationInput } from "../../src/lib/validation"; import { tokenHash } from "../../src/lib/tokens";
describe("helpers",()=>{it("normalizes input",()=>expect(clean("  Иван   Иванов ")).toBe("Иван Иванов"));it("pluralizes",()=>expect(places(22)).toBe("22 места"));it("hashes deterministically",()=>expect(tokenHash("x")).toBe(tokenHash("x")));it("normalizes email",()=>expect(registrationInput.parse({firstName:"Иван",lastName:"Иванов",city:"Москва",email:" TEST@EXAMPLE.COM ",eventIds:[1,2,3],idempotencyKey:"1b5b8f2d-e538-4b90-bc08-89c5b2d6d798"}).email).toBe("test@example.com"))});
